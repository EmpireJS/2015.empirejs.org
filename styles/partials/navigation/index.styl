@import 'grid/_definitions'
@import 'components/navigation/styles/_definitions'
@import '../../_definitions'
@import 'base'

.component-navigation

  /*&:after
    background-image linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.3),
      rgba(0, 0, 0, 0)
    )
    background-repeat no-repeat
    content ''
    height 5px
    left 0
    position absolute
    top 50px
    width 100%
    z-index 2*/
